package com.example.dashborad_pipe.Repo;

import com.example.dashborad_pipe.Model.Divers;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DiversRepo extends JpaRepository<Divers,Long> {
}
